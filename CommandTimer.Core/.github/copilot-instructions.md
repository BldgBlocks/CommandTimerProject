# Copilot Instructions

## Project Guidelines
- For this refactor, keep the ActionRelay lifecycle infrastructure, but have SettingsFlyoutViewModel use a direct override of the virtual deserialize hook instead of relying on ActionRelay listeners.